# Documentation images

These screenshots demonstrate Codex Skin Studio with real locally saved themes. The source image and video files visible in the screenshots are not distributed by this repository and are not covered by the project's MIT license.

When replacing these screenshots, verify that no authentication data, private logs, or sensitive system paths are visible. The gallery capture tool redacts sidebar project/task content by default; `--include-sidebar-content` should be used only when the screenshot owner has explicitly approved publishing that content.
